--- # copy mod.
- hosts: apacheweb
  user: test
  sudo: yes
  connection: ssh 
  gather_facts: no
  
  tasks: 
  - name: Copy from the file directory test file
    action: copy src=files/test2.txt dest=/home/test/test2.txt mode=222
  
